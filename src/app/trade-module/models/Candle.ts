export class Candle {
  private timestamp: string;
  private open: string;
  private close: string;
  private min: string;
  private max: string;
  private volume: string;
  private volumeQuote: string;
}
